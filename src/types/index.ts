import { ReactNode } from "react"

export type userDataStructute = {
  userid: string,
  fname: string,
  lname: string,
  email: string,
  badgeColor: string

}

export type childComponent = {
  children: ReactNode
}